/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: 'export', // <-- This tells Next.js to generate the static 'out' folder
};

export default nextConfig;
